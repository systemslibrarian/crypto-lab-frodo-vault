{
  "name": "crypto-lab-frodo-vault",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Interactive browser demo of FrodoKEM as a conservative plain-LWE post-quantum KEM",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.8.3",
    "vite": "^8.0.4",
    "vitest": "^4.1.4"
  }
}
